# SSD1306 driver for platformio / ESP-IDF

example code
```
 ssd1306_clean();
      ssd1306_print_str(0, 0, "LED change", false);
      ssd1306_print_str(0, 27, "Mapped Value:", false);
      ssd1306_print_str(0, 37, buffer, false);
      ssd1306_display();

```
